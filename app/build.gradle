plugins {
    id "com.android.application"
}

android {
    namespace "com.alppco.intermediatestock"
    compileSdk 35

    defaultConfig {
        applicationId "com.alppco.apphronline"
        minSdk 24
        targetSdk 35
        versionCode 160
        versionName "1.6.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
        }
    }
}
